---
description: Save a strong answer or insight back into the wiki as a page
argument-hint: [what to file, or leave blank to use the last answer]
---

You are running the **FILE-BACK** operation — the move that makes the wiki compound instead of merely accumulate. Authority: `wiki-base.md` §3 (Query → file-back) and this wiki's overlay (authorship rules).

**Content to file:** $ARGUMENTS  *(if empty, use the answer from the immediately preceding exchange.)*

Steps:

1. Choose the page type (synthesis, comparison, concept, or note) and apply the matching template from `TEMPLATES.md`.
2. Write it with citations back to the pages and sources it draws on `[src: ...]`. Label each register clearly: **sourced**, **synthesis**, **inference**.
3. Add typed `relates_to` edges (permitted vocabulary only) and update `index.md`.
4. **Authorship gate.** In a wiki with an authorship gate (e.g., a preserve-tension wiki), file this as `status: draft` / register `inference` and route it to `_state/review-queue.md` for my ratification before it becomes a `position` or `synthesis`. **Never present AI-authored synthesis as my position.** In a reconcile wiki, a well-cited additive page may be filed `active`.
5. Log it and commit.

Confirm what you filed, its status, and whether it's awaiting my ratification.
